[package]
name = "craole-cc-project"
version = "0.1.1"
description = "Cargo workspace and package scaffolding - part of the Craole CC development toolkit"
keywords = ["cargo", "workspace", "scaffold", "craole", "cc"]
readme = "README.md"
edition = { workspace = true }
authors = { workspace = true }
homepage = { workspace = true }
repository = { workspace = true }
license = { workspace = true }
categories = { workspace = true }

[lints]
workspace = true

[features]
default = [
  # "full",
  # "macros",
  #
]
full = ["tracing", "macros", "dep:cargo_metadata"]
tracing = ["dep:tracing"]
macros = []

[dependencies]
cargo_metadata = { workspace = true, optional = true }
dotenv = { workspace = true }
toml = { workspace = true }
tracing = { workspace = true, optional = true }

[dev-dependencies]

[[example]]
name = "basic"
required-features = []

# [[example]]
# name = "workspace"
# required-features = []

# [[example]]
# name = "advanced"
# required-features = ["tracing"]
